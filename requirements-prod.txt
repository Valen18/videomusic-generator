# Production dependencies for VideoMusic Generator
# Optimized for Docker deployment

# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0
aiohttp>=3.9.0

# Web application dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.0.0
python-multipart>=0.0.6

# AI/ML API clients
openai>=1.3.0
replicate>=0.15.0

# Media processing
Pillow>=10.0.0
numpy>=1.24.0

# Note: pygame and matplotlib are NOT included in production
# as they are only needed for the desktop GUI application
